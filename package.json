{
  "name": "engineering-loop-marketplace",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check": "node scripts/validate-package.mjs",
    "patch:check": "node patches/openai-codex-1.0.4-stale-guard/apply.mjs"
  },
  "engines": {
    "node": ">=18.18"
  }
}
